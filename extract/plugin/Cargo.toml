[package]
name = "extract_plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
aviutl2 = { version = "0.24.0", default-features = false, features = ["generic"] }
process_path = "0.1.4"
